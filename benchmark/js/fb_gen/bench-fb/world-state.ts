// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { Guild } from '../bench-fb/guild.js';
import { Item } from '../bench-fb/item.js';


export class WorldState {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):WorldState {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsWorldState(bb:flatbuffers.ByteBuffer, obj?:WorldState):WorldState {
  return (obj || new WorldState()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsWorldState(bb:flatbuffers.ByteBuffer, obj?:WorldState):WorldState {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new WorldState()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

worldId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

seed():string|null
seed(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
seed(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

guilds(index: number, obj?:Guild):Guild|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new Guild()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

guildsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

lootTable(index: number, obj?:Item):Item|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? (obj || new Item()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

lootTableLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startWorldState(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addWorldId(builder:flatbuffers.Builder, worldId:number) {
  builder.addFieldInt32(0, worldId, 0);
}

static addSeed(builder:flatbuffers.Builder, seedOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, seedOffset, 0);
}

static addGuilds(builder:flatbuffers.Builder, guildsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, guildsOffset, 0);
}

static createGuildsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startGuildsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addLootTable(builder:flatbuffers.Builder, lootTableOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, lootTableOffset, 0);
}

static createLootTableVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startLootTableVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endWorldState(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static finishWorldStateBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset);
}

static finishSizePrefixedWorldStateBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, undefined, true);
}

static createWorldState(builder:flatbuffers.Builder, worldId:number, seedOffset:flatbuffers.Offset, guildsOffset:flatbuffers.Offset, lootTableOffset:flatbuffers.Offset):flatbuffers.Offset {
  WorldState.startWorldState(builder);
  WorldState.addWorldId(builder, worldId);
  WorldState.addSeed(builder, seedOffset);
  WorldState.addGuilds(builder, guildsOffset);
  WorldState.addLootTable(builder, lootTableOffset);
  return WorldState.endWorldState(builder);
}
}
